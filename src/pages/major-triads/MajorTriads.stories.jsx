import React from 'react';
import { storiesOf } from '@storybook/react';
import { action } from '@storybook/addon-actions';

import MajorTriads from './MajorTriads.jsx';

storiesOf('MajorTriads', module)
  .addWithChapters('MajorTriads', {
    chapters: [{
      sections: [{
        sectionFn: () => ( )
      }]
    }]
  });
