import React from 'react';
import { storiesOf } from '@storybook/react';
import { action } from '@storybook/addon-actions';

import MainLayout from './MainLayout.jsx';

storiesOf('MainLayout', module)
  .addWithChapters('MainLayout', {
    chapters: [{
      sections: [{
        sectionFn: () => ( )
      }]
    }]
  });
